    name:     <!DOCTYPE html>
     <html lang="en">
<head>
  <meta charset="UTF-8">
  <title>EV0LVERSE Legal Dossier</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <header>
    <h1>EV0LVERSE Legal Dossier</h1>
    <p>All documents and evidence—the complete legal warfare package.</p>
  </header>
  <nav>
    <ul>
      <li><a href="Doc1_Federal_Complaint.pdf">Document 1: Federal Lawsuit</a></li>
      <li><a href="Doc2_Demand_Platform.pdf">Document 2: Demand Letter (Platform)</a></li>
      <li><a href="Doc3_Demand_GitHub_Microsoft.pdf">Document 3: Demand Letter (GitHub/MS)</a></li>
      <li><a href="Doc4_FBI_Complaint.pdf">Document 4: FBI Criminal Complaint</a></li>
      <li><a href="Doc5_Preservation_Notice.pdf">Document 5: Evidence Preservation Notice</a></li>
      <li><a href="Doc6_Estate_Petition.pdf">Document 6: Estate Petition</a></li>
      <li><a href="Doc7_Injunction_Motion.pdf">Document 7: Preliminary Injunction</a></li>
      <li><a href="Media_Kit.zip">Media Kit & Press Releases</a></li>
    </ul>
  </nav>
  <section>
    <h2>Key Evidence & Exhibits</h2>
    <ul>
      <li><a href="exhibits/git_logs.html">Git commit logs</a></li>
      <li><a href="exhibits/blockchain_records.html">Blockchain transaction proofs</a></li>
      <li><a href="exhibits/copilot_screenshots.zip">Copilot surveillance screenshots</a></li>
    </ul>
  </section>
  <footer>
    <p>© 2025 EV0LVERSE Legal Council. All rights reserved.</p>
  </footer>
</body>
</html>Build and Push BLEULION Mirror Agent Docker Image

on:
  push:
    branches:
      - feat/bleulion-treasury-codex
    paths:
      - 'tools/mirror/**'
      - 'tools/docker/**'
      - '.github/workflows/dockerize_mirror.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: bleulion-mirror

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix={{branch}}-
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./tools/mirror
          file: ./tools/docker/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Image digest
        run: echo "Image pushed with digest ${{ steps.build-and-push.outputs.digest }}"

      - name: Deployment instructions
        run: |
          echo "::notice::Docker image built and pushed successfully!"
          echo "::notice::Image: ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          echo "::notice::To deploy, run: docker pull ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:${{ github.sha }}"
          echo "::notice::Or use the latest tag: docker pull ${{ env.REGISTRY }}/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}:latest"

# Note: This workflow requires GITHUB_TOKEN which is automatically provided by GitHub Actions.
# No additional secrets need to be configured for pushing to ghcr.io.
#
# For deployment to production, you will need to set the following secrets in your environment:
# - ETH_PROVIDER: Ethereum RPC provider URL
# - CODEX_ENDPOINT: CODEX API endpoint
# - MIRROR_SIGNER_PRIVATE_KEY: Private key for signing mirror records (KEEP SECRET!)
# - PRIMARY_VAULT: Ethereum vault address to monitor
#
# These should be set as environment variables when running the container,
# NOT as GitHub Actions secrets in this workflow.
